pipeline {
    agent any
    tools {
        go 'go-1.16'
    }
    environment {
        GO116MODULE = 'on'
    }
    stages {
        stage('Compile') {
            steps {
                make binary
            }
        }
	stage('Test') {
            steps {
                echo "Running tests"
		// Tests require supported GPU
                // make test-main
                make check-format
            }
        }
    }
}
