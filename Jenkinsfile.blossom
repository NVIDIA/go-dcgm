pipeline {
    agent any
    tools {
        go 'go-1.16'
    }
 
    stages {
        stage('Compile') {
            steps {
                make binary
            }
        }
	stage('Test') {
            steps {
                echo "Running tests"
		// Tests require supported GPU
                // make test-main
                make check-format
            }
        }
    }
}
